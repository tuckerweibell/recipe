---
'@ezcater/recipe': major
---

BREAKING CHANGE: An upgrade to dependencies, like `react` (>= `16.14.0`) may be required with the addition of new dependencies (`mui/material`, `@emotion/react`, `@emotion/styled`, `@fortawesome/fontawesome-svg-core` and `@fortawesome/react-fontawesome`).
