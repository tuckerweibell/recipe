import {
  baseFontFamily as baseFontFamilyLegacy,
  baseFontSize as baseFontSizeLegacy,
  breakpoints as breakpointsLegacy,
  borderRadius as borderRadiusLegacy,
  borderWidth as borderWidthLegacy,
  colors as colorsLegacy,
  fonts as fontsLegacy,
  fontSizes as fontSizesLegacy,
  fontWeights as fontWeightsLegacy,
  lineHeights as lineHeightsLegacy,
  pageContentWidth as pageContentWidthLegacy,
  spacing as spacingLegacy,
} from '@recipe-ui/legacy-theme';

/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const baseFontFamily = baseFontFamilyLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const baseFontSize = baseFontSizeLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const breakpoints = breakpointsLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const borderRadius = borderRadiusLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const borderWidth = borderWidthLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const colors = colorsLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const fonts = fontsLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const fontSizes = fontSizesLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const fontWeights = fontWeightsLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const lineHeights = lineHeightsLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const pageContentWidth = pageContentWidthLegacy;
/**
 * @deprecated import from `@recipe-ui/legacy-theme` directly, or define styles local to your project.
 */
export const spacing = spacingLegacy;
