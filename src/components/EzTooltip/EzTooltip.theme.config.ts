import {mergeCss} from '../theme.config';

export default mergeCss({
  theme: {
    colors: {
      'tooltip-text': 'white',
      'tooltip-bg': '$gray800',
      'tooltip-border': '$gray800',
    },
    space: {
      'tooltip-padding': '$100',
    },
  },
});
