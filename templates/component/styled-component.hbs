import PropTypes from 'prop-types';
import styled from 'react-emotion';
import {base} from './{{ name }}.styles';
import {standard} from '../../themes';

/**
 * Describe {{ name }} here.
 */
const {{ name }} = styled('div')(base);

{{ name }}.propTypes = {
  /**
   * The theme controlling the component styles.
   */
  theme: PropTypes.shape({
    colors: PropTypes.object,
  }),
};

/**
 * defaultProps
 * @property {object} theme - uses the standard theme by default.
 */
{{ name }}.defaultProps = {
  theme: standard,
};

/**
 * @component
 */
export default {{ name }};
